name: DevSecOps CI/CD Pipeline

on:
  push:
    branch: main


jobs:
  build:
    runs-on: ubuntu-latest
  

    steps:
    - name: checkout repository
      uses: actions/checkout@v2

    - name: docker build image
      run: |
        docker build -t enjangse/fastapi:latest .
        docker images